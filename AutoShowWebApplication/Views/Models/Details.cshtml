@model AutoShowWebApplication.Model

@{
    ViewData["Title"] = "Details";
}

<h1>Деталі</h1>

<div>
    <h4>Модель</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ModelName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ModelName)
        </dd>
    </dl>
</div>
@if (User.IsInRole("admin"))
{
    <div>
        <a asp-action="Edit" asp-route-id="@Model.ModelId">Редагувати</a> |
        <a asp-action="Index">Повернутися до списку</a>
    </div>}

@if (!User.IsInRole("admin"))
{
    <div>
        
        <a asp-action="Index">Повернутися до списку</a>
    </div>}